# Anthropic SDK for Claude API
anthropic>=0.40.0

# HTTP requests (for simple fetching)
requests>=2.31.0

# Environment variable management
python-dotenv>=1.0.0

# File locking for parallel agents
filelock>=3.12.0
